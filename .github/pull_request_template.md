## What & Why

- **What changed?** 
- **Why?** 
- Closes #

## Evidence

<!-- Test output, or the command you ran against testnet and what it printed -->

## Local Checks

- [ ] `npm run typecheck`
- [ ] `npm run lint`
- [ ] `npm test`

## Published data (if applicable)

- [ ] **Output shape:** did an existing file under `public/v1/` change shape? Adding files or fields is fine; renaming, removing or retyping one needs a `v2` ([docs/outputs.md](../docs/outputs.md)).
- [ ] **Replay:** does this add or change a handler, so the published index needs a reset run after merge?
